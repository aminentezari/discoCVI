% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{make_moons}
\alias{make_moons}
\title{Utility Functions for DISCO Package}
\usage{
make_moons(n_samples = 100, noise = 0.05, random_state = NULL)
}
\arguments{
\item{n_samples}{Integer, number of samples to generate (divided between two moons)}

\item{noise}{Numeric, standard deviation of Gaussian noise. Default is 0.05.}

\item{random_state}{Integer, random seed for reproducibility. Default is NULL.}
}
\value{
A list with two components:
  \item{X}{A matrix with two columns (x, y coordinates)}
  \item{labels}{A vector of cluster labels (0 or 1)}
}
\description{
Creates a synthetic dataset with two interleaving half circles.
Useful for testing density-based clustering algorithms.
}
\details{
Helper functions for data generation, visualization, and validation.
Generate two moons dataset
}
\examples{
\dontrun{
# Generate data
data <- make_moons(n_samples = 300, noise = 0.05)
plot(data$X, col = data$labels + 1, pch = 19)

# Apply clustering
library(dbscan)
db <- dbscan(data$X, eps = 0.2, minPts = 5)
score <- disco_score(data$X, db$cluster - 1)
}
}
