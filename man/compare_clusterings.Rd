% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{compare_clusterings}
\alias{compare_clusterings}
\title{Compare multiple clusterings}
\usage{
compare_clusterings(X, clustering_list, min_points = 5)
}
\arguments{
\item{X}{A numeric matrix of features}

\item{clustering_list}{Named list of label vectors}

\item{min_points}{Integer, parameter for DISCO. Default is 5.}
}
\value{
A data frame with clustering names and their DISCO scores,
  sorted by score (best first)
}
\description{
Compute DISCO scores for multiple clustering results and rank them.
}
\examples{
\dontrun{
library(dbscan)
data <- make_moons(n_samples = 300)
X <- data$X

# Apply different algorithms
db <- dbscan(X, eps = 0.2, minPts = 5)
km <- kmeans(X, centers = 2)

# Compare
clusterings <- list(
  DBSCAN = db$cluster - 1,
  KMeans = km$cluster - 1
)
results <- compare_clusterings(X, clusterings)
print(results)
}
}
